'''a)Import a CSV file, summarize the file and plot any two numeric columns of the file.'''

path="D:/Brundha/DSR"
setwd(path)
dataval=read.csv("Marksheet.csv")
plot(dataval$subject1,dataval$subject2)



'''b)Import a CSV file using read.delim() function and add a suitable column of suitable name. 
Export this file which was modified as tab delimeted without row names to the working directory to R.'''

path="D:/Brundha/DSR"
setwd(path)
marks=read.delim("marksheet.csv")
val_new=vector(mode="numeric",length =length(marks$subject1))
marks$subject3<- seq.int(nrow(marks))
write.table(marks,file="newfile.csv", sep= "\t", row.names=FALSE)
marks


'''c)Factorize the income field in any csv file imported to different Levels by creating a new factor field 
and attach to the original File'''
